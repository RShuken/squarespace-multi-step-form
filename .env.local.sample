# Application Settings
FLASK_APP=app.py
FLASK_ENV=development
PORT=5001

# OpenAI Configuration (if used)
# OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (if needed)
# DATABASE_URL=postgresql://username:password@localhost:5432/your_database_name
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@localhost:5432/your_database_name

# Email Configuration (if needed)
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your_email@example.com
# MAIL_PASSWORD=your_email_password
# MAIL_DEFAULT_SENDER=your_email@example.com

# CORS Configuration
# CORS_ORIGINS=https://your-squarespace-site.com

# Session Configuration
# SECRET_KEY=your-secret-key-here

# Logging Level
# LOG_LEVEL=INFO

# Debug Mode (set to False in production)
# DEBUG=True

# Note: Never commit actual credentials to version control.
# This is a template. Copy this file to .env.local and fill in the values.
# The .env.local file is included in .gitignore
